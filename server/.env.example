PORT=5050
# Use "memory" if MongoDB is not installed locally.
# Or: mongodb://127.0.0.1:27017/ai_travel_planner
MONGO_URI=memory
JWT_SECRET=dev-change-this-jwt-secret
ADMIN_EMAIL=admin@travel.local
ADMIN_PASSWORD=Admin123!
ADMIN_NAME=Platform Admin

# Optional. Without a key, generate still works using Open-Meteo + a weather-aware fallback planner.
# GEMINI_API_KEY=
# OPENAI_API_KEY=
# LLM_PROVIDER=gemini
# LLM_MODEL=gemini-2.0-flash
